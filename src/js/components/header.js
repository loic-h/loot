import React from 'react';
import Looter from '../containers/looter';

const Header = () => (
  <div className="header">
    <div className="header__container">
      <Looter />
    </div>
  </div>
);

export default Header;
