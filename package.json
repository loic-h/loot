{
  "name": "loot",
  "version": "0.2.0",
  "description": "",
  "main": "app/index.js",
  "scripts": {
    "test": "NODE_ENV=test && mocha --exit",
    "start": "npm run clean && npm run front:build && npm run app:start",
    "app:start": "NODE_ENV=production && node app",
    "front:build": "mkdir -p static && webpack --mode production --config config/webpack.config.js",
    "dev": "npm run clean && (npm run front:dev & npm run app:dev)",
    "app:dev": "NODE_ENV=development && nodemon --watch app --watch config",
    "front:dev": "mkdir -p static && webpack --watch --mode development --config config/webpack.config.js",
    "clean": "rm -rf static"
  },
  "repository": {
    "type": "git",
    "url": "ssh://loiic/var/git/loot.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.4",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.4",
    "webpack": "^4.9.1",
    "webpack-cli": "^2.1.4"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "log4js": "^2.6.1",
    "mongoose": "^5.1.1",
    "npm": "^6.1.0",
    "pug": "^2.0.3",
    "update": "^0.7.4"
  }
}
